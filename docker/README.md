# Docker support for localhost development

`cp .env.example .env`

Then adjust the SSL_CERT_PATH to your domain (generally only the end has to be changed)

`docker-compose up -d`

And you should be running
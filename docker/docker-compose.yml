networks:
  siberian-network-ip:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.16.42.0/24
          gateway: 172.16.42.1

services:
  db:
    restart: always
    image: mariadb:latest
    ports:
      - 3306:3306
    volumes:
      - ./mysql:/var/lib/mysql
    env_file:
      - .env
    environment:
      MARIADB_ROOT_PASSWORD: "${PMA_PASSWORD}"
    networks:
      siberian-network-ip:
        ipv4_address: "${DB_IPV4}"

  php:
    restart: always
    image: siberian:php-fpm.7.3
    build:
      context: .
      dockerfile: ./php/Dockerfile
    volumes:
      - ../siberian:/var/www/html
      - ./php/docker.conf:/etc/php/7.3/fpm/pool.d/www.conf
      - ./php/php.ini:/etc/php/7.3/fpm/php.ini
      - "../modules:${PROJECT_PATH}/modules"
#    ports:
#      - "${SOCKETIO_RANGE}:${SOCKETIO_RANGE}"
    env_file:
      - .env
    networks:
      siberian-network-ip:
        ipv4_address: "${PHP_IPV4}"

  php82:
    restart: always
    image: siberian:php-fpm.8.2
    build:
      context: .
      dockerfile: ./php/php8.2.Dockerfile
    volumes:
      - ../siberian:/var/www/html
      - ./php/docker.conf:/etc/php/8.2/fpm/pool.d/www.conf
      - ./php/php.ini:/etc/php/8.2/fpm/php.ini
      - "../modules:${PROJECT_PATH}/modules"
    ports:
      - "${SOCKETIO_RANGE}:${SOCKETIO_RANGE}"
    env_file:
      - .env
    networks:
      siberian-network-ip:
        ipv4_address: "${PHP8_IPV4}"

  nginx:
    restart: always
    image: nginx:latest
    ports:
      - "${HTTP_PORT}:80"
      - "${HTTPS_PORT}:443"
    volumes:
      - ../siberian:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
#      - "${SSL_CERT_PATH}:/etc/nginx/ssl-certificates"
      - "../modules:${PROJECT_PATH}/modules"
    env_file:
      - .env
    networks:
      siberian-network-ip:
        ipv4_address: "${NGINX_IPV4}"

  pma:
    image: phpmyadmin
    restart: always
    ports:
      - 8181:80
    env_file:
      - .env
    networks:
      siberian-network-ip:
        ipv4_address: "${PHPMYADMIN_IPV4}"